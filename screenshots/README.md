# Screenshots

Drop your gameplay capture here as `gameplay.gif` — the project README embeds it.

Suggested clip (~8–10s, looping): a busy mid-run moment showing a level-up card
pick and the immediate power spike (tornado + multi-spear clearing a crowd).
Keep it ~600–800px wide, 15–24 fps, so the file stays small for the web.
